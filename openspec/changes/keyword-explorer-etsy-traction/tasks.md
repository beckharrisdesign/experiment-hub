# tasks — keyword-explorer-etsy-traction

## 1. User outcomes (from spec scenarios)

- [x] 1.1 A keyword with one matching listing shows its position
- [x] 1.2 A keyword with multiple ranking listings shows the best position
- [x] 1.3 A keyword with no ranking match shows a blank cell
- [x] 1.4 A keyword that is a tag on one listing shows its slot number
- [x] 1.5 A keyword tagged on multiple listings shows the earliest slot
- [x] 1.6 A keyword absent from every listing's tags shows a blank cell
- [x] 1.7 A minimum or maximum narrows the table
- [x] 1.8 A range filter applies to Ranked and Targeting the same as any other numeric column
- [x] 1.9 Status, Capture and Coverage are absent from the table
- [x] 1.10 The underlying data for all three is unchanged
- [x] 1.11 Blank cells sort after populated ones, not as zero

## 2. Prototype shell

- [x] 2.1 No `experiments/<slug>/prototype/` — hub platform work extending the shipped `keyword-explorer` route, same as that change itself (`rules/openspec-workflow.mdc` § Shared hub behavior). The shell is `app/keyword-explorer/page.tsx`.

## 3. Implementation

- [x] 3.1 `types/index.ts`: add `RankedMatch { best: number; matches: { listing: string; page: number; position: number }[] }` and `TargetingMatch { best: number; matches: { listingId: number; slot: number }[] }`. Extend `KeywordRow` with `ranked: RankedMatch | null` (static, from the corpus) and `targeting: TargetingMatch | null` (merged by `withTargeting()` — `lib/keyword-traction.ts`, see §6 — present on every row object, never `undefined`). `KeywordRow` itself never reaches `KeywordTable`: `toTableRows()` (added in round 7.1, §7) collapses both fields to `number | null` on the client-safe `KeywordTableRow`, which is what the component actually receives. (→ 1.1–1.6, 1.11)
- [x] 3.2 `scripts/ingest-pulls.py`: `build_corpus()` gains a pass reading every `erank-spotted-on-etsy` CSV in `docs/pulls/` — match `Search Term` against corpus keywords case-insensitive exact, attach `ranked` (best position + full listing/page/position list) onto the matching keyword row, `null` when no match. Wired into the existing `--apply` run; no new CLI flag. Regenerated `data/keyword-corpus.json` — 410 rows, all `ranked: null` (0 of 410 match the 12-row `erank-spotted-on-etsy` archive today, confirmed directly). (→ 1.1–1.3, 1.11)
- [x] 3.3 `lib/keyword-corpus.ts`: parse `ranked` from the raw (snake_case) corpus JSON into camelCase on `toRow()`, same pattern as `foundVia`. No behavior change to existing exports. (→ 1.1–1.3)
- [x] 3.4 `lib/keyword-traction.ts` (new): pure function `computeTargeting(keywords: string[], snapshots: RawListing[]): Map<string, TargetingMatch>` — case-insensitive exact match of each keyword against every snapshot's `tags` array, 1-based slot index, best = lowest slot across listings, absent keywords simply missing from the map (never a `0` entry). No I/O — unit-testable without Supabase, mirroring `lib/etsy-scorecard.ts`'s pure-functions precedent. (→ 1.4–1.6, 1.11)
- [x] 3.5 `app/keyword-explorer/page.tsx`: becomes an async server component; awaits `withTargeting()` (`lib/keyword-traction.ts`, moved there in round 6.1 — see §6) and renders its result. `withTargeting()` is the actual I/O wrapper: calls `getLatestListingSnapshots()` (`lib/etsy-sync.ts`) inside a try/catch, runs `computeTargeting` and merges each row's `targeting` value onto the statically-loaded corpus rows on success; on failure, every row's `targeting` is `null` and the failure is logged server-side only (`console.error`), no banner or partial-page error state (design.md § Decisions — silent degrade). (→ 1.4–1.6, design.md's Supabase-failure decision)
- [x] 3.6 `components/KeywordTable.tsx`: add `Ranked` and `Targeting` as right-aligned numeric sortable columns (blank cell, not `0`, when the row's value is `null`); remove `Status`, `Capture`, `Coverage` from the rendered `COLUMNS`/`<td>` set (props/row type keep carrying the fields — nothing is deleted from the data passed in). Column order: Keyword, Searches, Competition, KD, Ranked, Targeting, Found via (query), Searches/comp. — per `design.md` § User flow. `Badge` import dropped (only the removed Status column used it). (→ 1.9, 1.10, 1.11)
- [x] 3.7 `components/KeywordTable.tsx`: add the range-filter control — a column picker (`Select`; numeric columns: Searches, Competition, KD, Ranked, Targeting, Searches/comp.) plus min/max number inputs, one active range filter at a time (`design.md` § Decisions), combined with the existing keyword/capture/query filters in the `visible` `useMemo`. **Deviation from plan:** the min/max inputs are plain `<input type="number">` (matching the file's existing un-wrapped `<input type="search">` for the keyword filter), not MVDS `Field` — `Field` requires a `label` prop and renders its own layout chrome, heavier than this row's existing filter controls need. Blank (`null`) values are excluded by an active min bound and included when only a max bound is set, never coerced to `0`. (→ 1.7, 1.8)
- [x] 3.8 Sort comparator: `null` (blank) sorts after every populated value regardless of direction, on Ranked and Targeting specifically — not the generic numeric sort's default of treating missing as `0`/lowest. (→ 1.11)

## 4. QA

- [ ] 4.1 **Manual walkthrough, in Katy's terminal** (`pnpm dev` — needs 1Password for Supabase env, unlike the static parts of this route): confirm Ranked/Targeting render as numbers and sort correctly, the range filter narrows on a chosen column, Status/Capture/Coverage are gone from the table, and killing `SUPABASE_URL` locally degrades Targeting to blank without breaking the page. *(Not run from this session — no 1Password access here; the join logic, range filter, sort, and Supabase-failure fallback are all covered by 4.2's automated tests, but a real Targeting value against live production listings has not been eyeballed.)*
- [x] 4.2 Automated smoke, covering every Copilot review round documented below: `tests/keyword-corpus.test.ts` (fixture-based Ranked join, re-pull deduplication, `read_spotted_on_etsy_csv` row-keeping, `toRankedMatch` loader-boundary mapping — all fully synthetic fixtures, no dependency on live `docs/pulls/` content); `tests/lib/keyword-traction.test.ts` (`computeTargeting` + `withTargeting` + `toTableRows`); `tests/components/KeywordTable.test.tsx` (rendering, sort, range filter, and empty-state coverage); `tests/lib/etsy-snapshots.test.ts` (latest-capture filtering, TTL cache, in-flight coalescing). Deliberately not restating exact per-file case counts or a full-suite pass count here — both grow with every added round's coverage and would go stale on the next one; each numbered round section below (§5 onward) carries its own closing `pnpm exec vitest run` line, and the most recent one is authoritative. `pnpm exec eslint .` → 0 errors. `pnpm exec tsc --noEmit` → clean.

## 5. Copilot review round (2026-09-18)

Five real findings, all fixed and pushed; two more were self-caught while investigating one of them.

- [x] 5.1 **Real bug, highest severity of the round.** `read_spotted_on_etsy_csv`'s `int(... or 0)` fallback silently turned a blank Page/Position cell into the int `0` instead of dropping the row — the exact fabricated-zero failure mode `read_keyword_csv`'s own comment warns against, and a direct violation of this change's own "blank, never 0" rule (spec § Requirement: "A traction value with no match is visually and semantically distinct from a zero value"). Fixed: parse without a fallback (blank now raises `ValueError` and the row is dropped, matching `read_keyword_csv`), plus a new `page <= 0 or position <= 0` guard since a non-positive value is malformed, not a real low rank. Verified with a fixture CSV carrying two blank-field rows and one good row — confirmed only the good row survives. No change to `data/keyword-corpus.json`: the real `2026-09-17-erank-spotted-on-etsy.csv` has no blank Page/Position cells today, so this was a latent bug, not a live-data regression. (`scripts/ingest-pulls.py`)
- [x] 5.2 **Real gap.** `getLatestListingSnapshots()` returns the newest snapshot per listing across *all history*, so a deleted/deactivated listing keeps its last-known tags in that view — `computeTargeting` was reading all of them, meaning a keyword could show "Targeting" long after the listing that carried it stopped being live on Etsy. Fixed: `computeTargeting` now filters to `listing.state === "active"` before matching, mirroring the same field's use in `lib/etsy-listing-kit/evaluate.ts`. New tests: a tag on an `"inactive"` listing is excluded; a keyword still matches when at least one of several listings is active. (`lib/keyword-traction.ts`)
- [x] 5.3 **Real gap.** Targeting must be live on every request, but `app/keyword-explorer/page.tsx` never opted out of static rendering — `getLatestListingSnapshots()` is a Supabase read, not a Next.js dynamic API, so the segment was a static-rendering candidate that could bake `targeting: null` in at build time and never refresh. Fixed: `export const dynamic = "force-dynamic"`, matching the convention every other live-data page in this app already uses. (`app/keyword-explorer/page.tsx`)
- [x] 5.4 **Real gap.** The Supabase-failure silent-degrade path (design.md § Decisions) had no test exercising it — a regression could break it while every other test stayed green. Fixed: `withTargeting` exported (later moved to `lib/keyword-traction.ts` in round 2 — see 6.1); tests mock a rejected `getLatestListingSnapshots()` and assert every row's `targeting` comes back `null` without throwing, plus a happy-path test that a real match merges correctly. (`app/keyword-explorer/page.tsx`)
- [x] 5.5 **Real doc/implementation drift.** `design.md`'s Visual design table still said the range-filter inputs bind MVDS `Input`/`Field` and that `Badge` usage is unchanged — neither matches what shipped (plain `<input type="number">`; `Badge` removed along with Status). Fixed: row rewritten to state both deviations and why, rather than leaving an approved artifact silently wrong. (`design.md`)
- [x] 5.6 **Real test brittleness — self-caught while triaging 5.1–5.5, not a Copilot comment.** `tests/keyword-corpus.test.ts` asserted "every row's `ranked` is `null` today," which is the archive's current, transient state, not an invariant — the first future Spotted on Etsy pull that overlaps the corpus would fail this test despite correct behavior. This is the identical brittleness pattern #501 already fixed once (hardcoded snapshot instead of an invariant). Fixed: replaced with a shape check (null, or a well-formed `{best, matches}` where `best` equals the minimum position in `matches`) that holds regardless of how many rows currently match. (`tests/keyword-corpus.test.ts`)
- [x] 5.7 **Real doc inaccuracy — also self-caught, not a Copilot comment.** `proposal.md` claimed Targeting's live Supabase read is "the same read `etsy-zero-sales-funnel`'s scorecard page already ships in production." Checked: that page was never wired up (`etsy-zero-sales-funnel/tasks.md` §3.2 is still open) — the read exists and is unit-tested, but this change is its first live caller, not a second one. Fixed: corrected in place with the finding noted, rather than quietly rewritten. (`proposal.md`)
- [x] 5.8 **Test-quality fix — Copilot-suppressed (not separately posted), not a functional bug.** `KeywordTable.test.tsx`'s "no Status/Capture/Coverage column" test queried exact uppercase strings ("STATUS") against title-case rendered headers ("Status") — it would pass even if the columns were never removed, since that exact string was never going to render either way. Fixed: queries `columnheader` role elements case-insensitively, so a regression that reintroduced a column would actually be caught. (`tests/components/KeywordTable.test.tsx`)

Re-ran full suite + lint + typecheck after all of the above: `pnpm exec vitest run` → 1240 passed / 49 skipped (2 pre-existing unrelated failures), `pnpm exec eslint .` → 0 errors, `pnpm exec tsc --noEmit` → clean.

## 6. Copilot re-review round (2026-09-18, second pass)

A fresh review requested after round 5's fixes. Eight more findings — three posted inline, five in the review body. All real except one, which was a genuine spec-wording ambiguity rather than a code defect; fixed by clarifying the spec instead of degrading working UI.

- [x] 6.1 **Real, high-value catch.** `withTargeting` was exported from `app/keyword-explorer/page.tsx` — a Next.js `page.tsx` is type-checked as a route module and may only export the default component plus recognised segment config (`metadata`, `dynamic`, …); an arbitrary named export there fails Next 16's generated route-type check on `next build` even though `tsc`/`vitest` (which don't run that check) stayed clean. Confirmed this repo is on `next@^16.2.10`. Fixed: `withTargeting` moved to `lib/keyword-traction.ts`; `page.tsx` now only exports `default`, `metadata` and `dynamic`. (`app/keyword-explorer/page.tsx`, `lib/keyword-traction.ts`)
- [x] 6.2 **Real hardening, same finding.** While moving `withTargeting`, its catch path returned the input `rows` unchanged on a Supabase failure rather than explicitly nulling `targeting` — harmless today (the one call site always passes freshly-loaded rows with `targeting: null` already), but not guaranteed once the function is an importable lib export a future caller could hand real Targeting data to. Fixed: catch path now maps every row to `targeting: null` explicitly. New test: a row that already carried a real Targeting value still comes back blank after a failed read. (`lib/keyword-traction.ts`)
- [x] 6.3 **Real gap.** `read_spotted_on_etsy_csv`'s blank/non-positive guard (round 5.1) had no committed test — I'd verified it with a one-off script, not permanent coverage. Fixed: new fixture-based describe block (`read_spotted_on_etsy_csv row-keeping, against a hand-built fixture`) with blank-Position, blank-Page, non-positive-Position and one valid row, asserting only the valid one survives. (`tests/keyword-corpus.test.ts`)
- [x] 6.4 **Real doc drift I'd missed on the first pass.** I fixed the Visual design table's MVDS `Field` mention in round 5.5 but missed an identical mention in the Decisions section two paragraphs down — same artifact, same contradiction, still present. Fixed: reworded to point at the Libraries/version row instead of restating (and this time getting wrong) the same fact twice. Also cleaned up an unedited thinking-out-loud fragment ("...status filter is gone too (see below), so...") left in the same paragraph from an earlier draft. (`design.md`)
- [x] 6.5 **Real loader-boundary gap.** `toRankedMatch`'s snake_case→camelCase mapping was only exercised end-to-end via `loadKeywordCorpus()` against the live corpus, which is all `ranked: null` today — the non-null branch (the actual field-by-field copy) had no direct test, so a typo'd field name would stay invisible until a real pull overlaps the corpus. Fixed: exported `toRankedMatch`; new test calls it directly with a hand-built non-null `RawRankedMatch` and asserts every field survives. (`lib/keyword-corpus.ts`, `tests/keyword-corpus.test.ts`)
- [x] 6.6 **Real fixture brittleness, same class as 5.6.** The Ranked-join fixture test (`tests/keyword-corpus.test.ts`) `copyFileSync`'d the real, committed `2026-09-17-erank-spotted-on-etsy.csv` into its temp dir rather than building its own — so a future re-pull changing "snow globe"'s real positions (or the file simply being superseded) would break this test despite correct behavior. Fixed: replaced with a fully synthetic Spotted on Etsy CSV the test owns outright, matching the hand-built-fixture pattern already used elsewhere in this file. Exact-value assertions (`.toBe(8)`) are fine again now that the fixture is fully test-owned, not live-data-dependent. (`tests/keyword-corpus.test.ts`)
- [x] 6.7 **Verified, not a defect — spec wording was genuinely ambiguous, fixed the spec instead of the code.** `formatTraction` renders an em dash for a `null` Ranked/Targeting value, and the spec's Scenario text said a blank cell must never show "0 *or any other placeholder value*" — read literally, that forbids the dash. But the em dash is this exact codebase's own pre-existing convention (`formatRatio`, same file, for `Searches / comp.` when competition is zero), and the Requirement's own summary line only ever promised the value stays "visually and semantically distinct from a zero value" — a dash satisfies that; a truly empty `<td>` would arguably read worse. Fixed: clarified both Scenario/Fails-until lines in `spec.md` to explicitly bless a non-numeric placeholder glyph, scoping the invariant to the underlying value (never rendering, sorting, or exporting as `0`) rather than to the DOM being literally empty. No code changed. (`specs/keyword-traction/spec.md`)
- [x] 6.8 **Consolidation, not a Copilot finding.** Moving `withTargeting` (6.1) made `tests/app/keyword-explorer-page.test.ts` test a function that no longer lives in `app/`. Merged its two tests into `tests/lib/keyword-traction.test.ts` alongside `computeTargeting`'s, and removed the now-empty `tests/app/` directory. (`tests/lib/keyword-traction.test.ts`)

Re-ran full suite + lint + typecheck after all of the above: `pnpm exec vitest run` → **1248 passed** / 49 skipped (same 2 pre-existing unrelated failures), `pnpm exec eslint .` → 0 errors, `pnpm exec tsc --noEmit` → clean.

## 7. Copilot re-review round (2026-09-18, third pass)

A fresh review after round 6's fixes. Six more findings — one posted inline (the most significant of the whole PR), five in the review body (two flagged "previously missed," meaning present since round 1 but only surfaced now).

- [x] 7.1 **Real, most significant finding of the PR — public data exposure.** `KeywordTable` is a client component on `/keyword-explorer`, which has no auth gate (confirmed against `middleware.ts`: only `/admin`, `/experiments` edit-cookie logic, and `pdf-metadata-viewer` are gated). The original implementation passed full `KeywordRow` objects straight to it, so `ranked.matches` (ranked listings' titles, pages, positions) and `targeting.matches` (live listing IDs and which of the 13 tag slots they occupy) serialized into the RSC payload for every anonymous visitor — even though the table only ever renders `.best`. Fixed: new `KeywordTableRow` type (`types/index.ts`) with `ranked`/`targeting` as plain `number | null`; new `toTableRows()` (`lib/keyword-traction.ts`) collapses `KeywordRow[]` to it right before the page renders; `KeywordTable` and `lib/keyword-corpus.ts`'s `demandRatio`/`totalTagOccurrences` narrowed to accept the leaner shape. Full detail stays server-side, available for a future per-row surface (Non-Goals) that just never ships to the browser today. Documented in `design.md` § Decisions. (`types/index.ts`, `lib/keyword-traction.ts`, `lib/keyword-corpus.ts`, `components/KeywordTable.tsx`, `app/keyword-explorer/page.tsx`)
- [x] 7.2 **Real gap, deepens round 6.2's fix.** `getLatestListingSnapshots()`'s source view (`etsy_latest_listing_snapshots`) is newest-row-per-listing across *all history* — `state === "active"` alone can't see a listing Etsy stopped returning entirely, since its last snapshot (and whatever `state` it had then) stays in the view forever. Investigated `experiments/etsy-notion-sync/prototype/store_supabase.py`'s `latest_from_current_capture` (the Python side of this same pipeline already solves this) and ported the same rule to TypeScript: `getLatestListingSnapshots()` now also selects `captured_at` and filters to rows stamped with the batch's newest value. `state === "active"` in `computeTargeting` stays as a second, narrower layer (a listing present in the latest capture but marked draft/inactive still shouldn't count). Updated the existing, previously-passing `tests/lib/etsy-snapshots.test.ts` suite (select-string assertion, all mock rows now carry `captured_at`) and added a case for a stale listing being dropped. (`lib/etsy-sync.ts`, `tests/lib/etsy-snapshots.test.ts`)
- [x] 7.3 **Real, previously-missed UX bug.** The range filter's min/max inputs forced `inputMode="numeric"` (integer keypad on mobile) even when the active column was `Searches / comp.`, whose values are fractional (`demandRatio()` returns e.g. `1.522`) — no way to type a decimal point on a touch keyboard. Fixed: `inputMode` switches to `"decimal"` when `rangeColumn === "ratio"`. (`components/KeywordTable.tsx`)
- [x] 7.4 **Real coverage gap.** Every sort/range test exercised Ranked only; Targeting has its own parallel branch in both the sort comparator and `rangeValue()`, so a Targeting-specific regression (wrong field read, blank-ordering broken) could pass the whole suite. Fixed: added a 4th fixture row (targeted but not ranked) and Targeting-specific sort and min/max range tests mirroring the existing Ranked ones. (`tests/components/KeywordTable.test.tsx`)
- [x] 7.5 **Real fixture gap.** The Ranked-join fixture used identical casing ("snow globe") on both the keyword CSV and the Spotted on Etsy `Search Term`, so the join's case-insensitive match was never actually exercised — a regression to case-sensitive matching would still pass. Fixed: fixture's `Search Term` is now `"Snow Globe"` against the keyword CSV's lowercase `"snow globe"`. (`tests/keyword-corpus.test.ts`)
- [x] 7.6 **Consolidation, not a Copilot finding.** Added a direct unit test for the new `toTableRows()` (non-null collapse, null-stays-null) alongside `computeTargeting`'s and `withTargeting`'s tests. (`tests/lib/keyword-traction.test.ts`)

Re-ran full suite + lint + typecheck after all of the above: `pnpm exec vitest run` → **1254 passed** / 49 skipped (same 2 pre-existing unrelated failures), `pnpm exec eslint .` → 0 errors, `pnpm exec tsc --noEmit` → clean.

## 8. Copilot re-review round (2026-09-18, fourth pass)

A fresh review after round 7. Two findings, both real, both small.

- [x] 8.1 **Real repo-convention violation.** `rules/component-conventions.mdc` requires every component under `components/` to define a named `<ComponentName>Props` interface, not an inline destructured type — `KeywordTable`'s signature was `{ rows }: { rows: KeywordTableRow[] }`, an outlier against that documented rule. Fixed: added `interface KeywordTableProps { rows: KeywordTableRow[] }`. (`components/KeywordTable.tsx`)
- [x] 8.2 **Real stale comment, introduced by round 7's own fix.** A test comment in `tests/lib/keyword-traction.test.ts` still explained `getLatestListingSnapshots()` as returning rows "across all history" — true before round 7.2, false after: that function now narrows to the latest capture itself. The test this comment sits on exercises a different, still-real guard (`computeTargeting`'s own `state === "active"` check, for a listing captured in the latest run but not live/sellable) — the comment now contradicted the code next to it. Fixed: reworded to describe what this specific test actually guards. (`tests/lib/keyword-traction.test.ts`)

Re-ran full suite: `pnpm exec vitest run` → 1254 passed / 49 skipped (same 2 pre-existing unrelated failures), `pnpm exec eslint .` → 0 errors, `pnpm exec tsc --noEmit` → clean.

## 9. Copilot re-review round (2026-09-18, fifth pass)

A fresh review after round 8. Lower-confidence tier this time ("🔵 Needs a closer look," vs. every prior round's "🟡 Changes recommended") — 3 findings, 0 posted as separate threads (all suppressed). Two real, fixed; one investigated and judged not a defect against this PR's actual mandate.

- [x] 9.1 **Real coverage gap.** Range-filter tests only exercised Ranked and Targeting; Searches, Competition, KD and the fractional ratio each have their own `rangeValue()` branch and were untested through the UI (though `demandRatio` itself has unit coverage elsewhere). Fixed: gave `ROWS` real per-row `competition`/`kd` variation (previously every row shared the same defaults, which would have made a Competition/KD range test meaningless even if written) plus a fifth row with `competition: 0` specifically for the ratio-null case; added one representative range test per remaining column. (`tests/components/KeywordTable.test.tsx`)
- [x] 9.2 **Real test-hygiene bug.** `tests/lib/keyword-traction.test.ts`'s `withTargeting` describe block installs a `console.error` spy in `beforeEach` but never restores it — `mockReset()` only touches the Supabase mock. Left unfixed, the spy stays installed after this suite finishes and can silently swallow real `console.error` calls in a later suite sharing the same Vitest worker. Fixed: added `afterEach(() => vi.restoreAllMocks())`. (`tests/lib/keyword-traction.test.ts`)
- [ ] 9.3 **Investigated, not fixed — verified as matching this pipeline's existing accepted design, not a defect this PR introduced.** Finding: `getLatestListingSnapshots()`'s `newest` computation only considers `captured_at` values actually present in the returned rows, so a capture run that completes with *zero* listing rows (not "no rows at all," but "older rows exist, the newest run just wrote none") would have the function silently treat the next-older batch as current. Proposed fix: cross-reference `etsy_runs` for the authoritative latest successful run. Checked against `experiments/etsy-notion-sync/prototype/store_supabase.py`'s `latest_from_current_capture` — the exact function round 7.2 was explicitly asked to port and mirror — and confirmed it has the identical limitation: it also only reasons about `captured_at` values present in whatever rows it's handed, with no `etsy_runs` cross-reference. Its own docstring's stated safety argument ("dropping rows not at the newest timestamp is the safe direction... the next full capture restores them") already covers exactly this case: falling back to the last real data is the intended, accepted behavior for a run that wrote nothing, not a gap. Implementing the proposed fix would diverge the TS side from the Python source of truth it was explicitly asked to match, and would pull `etsy_runs` into a function that has zero live callers today — real hardening, but of the shared sync pipeline's edge-case handling in general, not of this PR's actual feature (the Keyword Explorer traction columns). Left as a known characteristic, not a defect, and out of scope for this change; a good candidate for its own follow-up against `etsy-sync.ts` broadly (both the TS and Python sides) if Katy wants it.

Re-ran full suite: `pnpm exec vitest run` → **1258 passed** / 49 skipped (same 2 pre-existing unrelated failures), `pnpm exec eslint .` → 0 errors, `pnpm exec tsc --noEmit` → clean.

## 10. Copilot re-review round (2026-09-18, sixth pass)

A fresh review after round 9. Same lower-confidence tier as round 9 ("🔵 Needs a closer look") — 3 findings, 0 posted as separate threads (all suppressed). All three real.

- [x] 10.1 **Real, most significant finding of the round — a second, distinct data-exposure vector on top of round 7.1's fix.** Round 7.1 collapsed `KeywordTable`'s *props* to `KeywordTableRow` so the RSC payload no longer carries per-listing detail — but `KeywordTable.tsx` still imported `demandRatio`/`totalTagOccurrences` from `lib/keyword-corpus.ts`, and that module has a top-level `import raw from "@/data/keyword-corpus.json"`. A client component's imports pull their entire module graph into the browser JS bundle at build time, independent of what's actually passed as props — so the full corpus JSON (including any future non-null `ranked.matches` listing detail) was still shipping to anonymous visitors, just via the bundle graph instead of the RSC payload. Fixed: extracted `demandRatio`/`totalTagOccurrences` into a new `lib/keyword-metrics.ts` with no JSON import and no other server-only dependency; `KeywordTable.tsx` and `tests/keyword-corpus.test.ts` now import them from there. `lib/keyword-corpus.ts` keeps only the corpus-loading functions that legitimately need the static import. (`lib/keyword-metrics.ts`, `lib/keyword-corpus.ts`, `components/KeywordTable.tsx`, `tests/keyword-corpus.test.ts`)
- [x] 10.2 **Real edge case in round 7.2's own fix.** `getLatestListingSnapshots()`'s newest-`captured_at` reduction stays `null` when no row has a timestamp — and the subsequent filter (`row.captured_at === newest`) then degenerates to `row.captured_at === null`, matching *every* null-timestamp row and passing all of them through as "current." An all-null batch (every row missing its capture timestamp) should resolve to no rows, since there is no way to identify which one is actually latest — not to every row. Fixed: return `[]` immediately when `newest` is `null` after the reduce, before the filter runs. New test: a batch where every row's `captured_at` is `null` now returns an empty array. (`lib/etsy-sync.ts`, `tests/lib/etsy-snapshots.test.ts`)
- [x] 10.3 **Real doc drift, self-caught while fixing 10.1 — not a Copilot comment.** Task 4.2 above said "final state after both Copilot rounds," true when it was written after round 6 but stale by three more rounds since; it also undercounted `tests/components/KeywordTable.test.tsx` (said 6, actually 13 after rounds 7.4 and 9.1's additions) and `tests/lib/keyword-traction.test.ts` (said 12, actually 13 after round 7.6's `toTableRows` test). Fixed: reworded to point at §§5–9 rather than restating a count that goes stale every round, and corrected both numbers. (`openspec/changes/keyword-explorer-etsy-traction/tasks.md`)

Re-ran full suite + lint + typecheck after all of the above: `pnpm exec vitest run` → **1259 passed** / 49 skipped (same 2 pre-existing unrelated shallow-clone failures), `pnpm exec eslint .` → 0 errors (9 pre-existing unrelated warnings elsewhere), `pnpm exec tsc --noEmit` → clean.

## 11. Copilot re-review round (2026-09-18, seventh pass)

A fresh review after round 10. Same lower-confidence tier ("🔵 Needs a closer look") — 2 findings, 0 posted as separate threads (all suppressed). Both real.

- [x] 11.1 **Real production-hardening gap, introduced by this PR itself.** `/keyword-explorer` was a fully static route before this change; round 5.3 made it `export const dynamic = "force-dynamic"` so Targeting stays live, which means every anonymous page load now triggers its own service-role Supabase read of `etsy_latest_listing_snapshots` with no cache or throttle — a crawler or a burst of refreshes multiplies that load with nothing to bound it, unlike the existing precedent for exactly this shape of risk (`app/etsy-listing-kit/api/evaluate/route.ts`'s per-listing cache + per-IP throttle, design decision 7 there). Fixed: `getLatestListingSnapshots()` now holds a 60-second, per-server-instance TTL cache (same posture as the ELK precedent — module-scope, not a global guarantee on serverless), documented inline with the freshness trade-off. A failed read is never cached, so the next request still gets a real retry rather than a silently-repeating error. New tests: a second call within the TTL doesn't re-hit Supabase; clearing the cache (test-only export) forces a real re-read; a failed read isn't cached. (`lib/etsy-sync.ts`, `tests/lib/etsy-snapshots.test.ts`)
- [x] 11.2 **Real doc drift, previously missed — present since round 5.3's `page.tsx` change but only surfaced now.** `specs/keyword-traction/spec.md`'s "Status, Capture and Coverage are no longer visible table columns" requirement listed the visible column order as Keyword, Searches, Competition, KD, Found via, Searches/comp., Ranked, Targeting — but the shipped `COLUMNS` array in `components/KeywordTable.tsx` (and `design.md`'s own IA section) places Ranked and Targeting immediately after KD, ahead of Found via and Searches/comp. Fixed: corrected the requirement's column order to match what's actually rendered. (`openspec/changes/keyword-explorer-etsy-traction/specs/keyword-traction/spec.md`)

Re-ran full suite + lint + typecheck after all of the above: `pnpm exec vitest run` → **1262 passed** / 49 skipped (same 2 pre-existing unrelated shallow-clone failures), `pnpm exec eslint .` → 0 errors (9 pre-existing unrelated warnings elsewhere), `pnpm exec tsc --noEmit` → clean.

## 12. Copilot re-review round (2026-09-18, eighth pass)

A fresh review after round 11. Back to "🟡 Changes recommended" — 5 findings, 1 posted inline (the round's most significant, on round 11's own cache fix), 4 suppressed. All five real.

- [x] 12.1 **Real gap in round 11.1's own fix — posted inline, not suppressed.** `snapshotCache` is only populated after the awaited Supabase read completes, so concurrent requests arriving while the first read is still in flight — the exact burst the cache exists to absorb — all see no cache yet and each start their own service-role query, defeating the TTL's load bound for the case that matters most. Fixed: added `snapshotFetchInFlight`, a single in-flight promise every concurrent caller within the same window now awaits instead of starting a duplicate Supabase read; cleared in a `finally` whether the read succeeds or throws, so a failure still lets the next call (concurrent or not) get a real retry rather than hanging on a dead promise. New test: three concurrent calls via `Promise.all` resolve to the same result from exactly one Supabase read. (`lib/etsy-sync.ts`, `tests/lib/etsy-snapshots.test.ts`)
- [x] 12.2 **Real bug, self-contained to this round's own area — repeat pulls silently duplicated matches.** `build_ranked_index()` appended every Spotted on Etsy CSV row directly to a term's `matches` list with no dedup, but `RankedListingMatch` carries no capture identifier — so re-pulling the same term/listing (an intentional, expected part of this archive per proposal.md § Why) grew `matches` by one duplicate entry per re-observation instead of updating the existing one; the promised "every ranking listing" detail would show the same listing more than once, and `best` stayed correct only by coincidence (min over a list that includes duplicates of itself). Fixed: deduped by listing name per term, keeping the better (lower) position observed for that listing across every archived pull. New tests: two captures of the same listing collapse to one match at the better position; a genuinely different listing in only one capture still gets its own entry; `best` is computed over the deduped set. (`scripts/ingest-pulls.py`, `tests/keyword-corpus.test.ts`)
- [x] 12.3 **Real UX bug.** The empty-state message ("No rows match these filters. The archive is hand-filtered at capture time...") is accurate for a keyword/capture/query filter turning up nothing, but a range filter producing zero rows is a direct, known consequence of the bound Katy set — showing the archive-omission explanation there misattributes the cause. Fixed: the range-filter case now gets its own copy ("No rows fall within this range. Widen or clear it to see more."); the archive-omission copy stays for every other empty-result case. New tests for both branches. (`components/KeywordTable.tsx`, `tests/components/KeywordTable.test.tsx`)
- [x] 12.4 **Real test-coverage gap.** The Ranked/Targeting sort tests only asserted that blank rows sort last in both directions — never that the *populated* values are actually in the right order, so a comparator that sorted numeric values backwards while still pushing blanks last would pass unnoticed. Fixed: both tests now also assert the exact order of the three populated rows in each direction. (`tests/components/KeywordTable.test.tsx`)
- [x] 12.5 **Real doc drift.** `computeTargeting`'s doc comment said "the caller (the Keyword Explorer page) owns fetching snapshots via `getLatestListingSnapshots()`" — true before round 6.1 moved `withTargeting` into this same file, false after: `withTargeting` is the actual I/O wrapper now, and the page never calls `getLatestListingSnapshots()` directly. Fixed: reworded to name `withTargeting` as the I/O boundary. (`lib/keyword-traction.ts`)

Also fixed, self-caught while addressing 12.1: task 4.2's QA line was stale again (it pointed at "§§5–9" for the current pass count, already wrong once §§10–11 landed) — reworded to point at "the most recent round section's closing line" generically rather than a specific section number, so it can't drift on the next round the same way. (`openspec/changes/keyword-explorer-etsy-traction/tasks.md`)

Re-ran full suite + lint + typecheck after all of the above: `pnpm exec vitest run` → **1268 passed** / 49 skipped (same 2 pre-existing unrelated shallow-clone failures), `pnpm exec eslint .` → 0 errors (9 pre-existing unrelated warnings elsewhere), `pnpm exec tsc --noEmit` → clean.

## 13. Copilot re-review round (2026-09-18, ninth pass)

Back to "🟡 Changes recommended" — 8 findings: 1 posted inline, 7 suppressed. All eight real.

- [x] 13.1 **Real bug, posted inline.** The range-filter empty-state message keyed off `rangeColumn !== NO_RANGE` alone — but merely picking a range *column* isn't an applied filter; `passesRange` treats a both-blank min/max as no bound at all. Picking "Ranked" and then having an unrelated filter (keyword, capture, status) empty the table showed the range-specific copy even though no range was ever applied. Fixed: added a `rangeBoundSet` check (`rangeColumn !== NO_RANGE` *and* a non-empty min or max) and keyed the branch off that instead. New test: picking a range column with no bound set, then emptying the table via the keyword filter, still shows the archive-omission copy. (`components/KeywordTable.tsx`, `tests/components/KeywordTable.test.tsx`)
- [x] 13.2 **Real test-coverage gap in round 11's own cache.** The suite covered a cache hit and the test-only manual reset, but nothing ever advanced past the 60-second TTL itself — a regression that made the cache effectively permanent (e.g. a flipped comparison) would still pass every test in the block. Fixed: added a fake-timer test (`vi.useFakeTimers()`/`vi.advanceTimersByTime()`, scoped to this describe block per `rules/vitest-conventions.mdc`) that crosses the TTL and asserts a second real Supabase read. (`tests/lib/etsy-snapshots.test.ts`)
- [x] 13.3 **Real repo-convention violation.** `rules/vitest-conventions.mdc` requires `vi.clearAllMocks()` in `beforeEach`; `withTargeting`'s describe block instead called `.mockReset()` on one specific mock, which would silently miss clearing any other mock added to this module later. Fixed: switched to `vi.clearAllMocks()`. (`tests/lib/keyword-traction.test.ts`)
- [x] 13.4 **Real doc drift.** `KeywordTable.tsx`'s column-definitions comment claimed "nothing here reads or discards" the hidden Status/Capture/Coverage fields, but the component's existing Capture and Status filter controls do read `capture` and `current`. Fixed: reworded to say the fields stay available to those filters, only their own columns are gone. (`components/KeywordTable.tsx`)
- [x] 13.5 **Real doc drift in `design.md` § Goals.** Still promised "Targeting reads live listing tags on every request," no longer true once round 11 added the 60-second cache. Fixed: reworded to state the cache's freshness bound instead of per-request freshness. (`openspec/changes/keyword-explorer-etsy-traction/design.md`)
- [x] 13.6 **Real doc drift in `design.md` § Decisions.** The Targeting decision still described the route's server component calling `getLatestListingSnapshots()`/`computeTargeting()` directly — the actual architecture since round 6.1 has `withTargeting()` (`lib/keyword-traction.ts`) own that I/O, with the page only awaiting it. Fixed: reworded to name `withTargeting()` as the I/O boundary, and added a Risks/Trade-offs entry for the cache's own freshness trade-off (a dangling "Risks, below" reference this same edit introduces, now backed by real content). (`openspec/changes/keyword-explorer-etsy-traction/design.md`)
- [x] 13.7 **Real doc drift in `tasks.md` task 3.5.** Same architecture drift as 13.6, in the implementation record: task 3.5 said `page.tsx` calls `getLatestListingSnapshots()` directly in a try/catch. Fixed: reworded to describe `page.tsx` awaiting `withTargeting()`, which owns the actual Supabase call and try/catch. (`openspec/changes/keyword-explorer-etsy-traction/tasks.md`)
- [x] 13.8 **Consolidation of 13.2's finding, restated for the duplicate location.** The same TTL-expiry gap was flagged against both `lib/etsy-sync.ts:105` (the cache check itself) and `tests/lib/etsy-snapshots.test.ts:176` (the existing hit test) — one fix (13.2) addresses both.

Re-ran full suite + lint + typecheck after all of the above: `pnpm exec vitest run` → **1270 passed** / 49 skipped (same 2 pre-existing unrelated shallow-clone failures), `pnpm exec eslint .` → 0 errors (9 pre-existing unrelated warnings elsewhere), `pnpm exec tsc --noEmit` → clean.

## 14. Copilot re-review round (2026-09-18, tenth pass)

7 findings: 1 posted inline, 6 suppressed. Six real fixes; one investigated and documented as an accepted, unfixable-from-this-data-source limitation rather than a phantom fix.

- [x] 14.1 **Real bug, the round's most significant.** The range-filter min/max inputs were global state, not per-column: setting Ranked ≥ 10 and then switching the range column to Targeting silently kept applying ≥ 10 to Targeting — a bound Katy never set on that column. Fixed: selecting a new range column now clears both bounds. New test: setting a bound on Ranked, switching to Targeting, confirms the inputs are empty and every row is visible again (no stale bound carried over). (`components/KeywordTable.tsx`, `tests/components/KeywordTable.test.tsx`)
- [x] 14.2 **Real doc drift, self-inflicted by round 11's own cache addition.** The cache's doc comment still justified itself by citing "Targeting must reflect the current tags on every request" — true before the cache, false after (that's now a ≤60-second bound, not per-request). Fixed: reworded to describe `force-dynamic` as ruling out build-time baking, and to state the cache's actual freshness trade-off. (`lib/etsy-sync.ts`)
- [x] 14.3 **Real doc drift in `design.md` § Decisions, same root cause as round 13's 13.6.** The Supabase-failure decision still said "the server component catches it" — that's `withTargeting()` (`lib/keyword-traction.ts`), not `page.tsx`, since round 6.1. Fixed: named `withTargeting()` as the actual catch boundary. (`openspec/changes/keyword-explorer-etsy-traction/design.md`)
- [x] 14.4 **Real doc drift in `proposal.md`.** The Targeting paragraph still described the page calling `getLatestListingSnapshots()` directly and left "design.md decides the fetch/caching shape" as an open future question — both settled since (round 6.1's `withTargeting()` move, round 7's cache). Fixed: reworded to describe what's actually shipped. (`openspec/changes/keyword-explorer-etsy-traction/proposal.md`)
- [x] 14.5 **Real doc drift in `tasks.md` task 3.1, same root cause as 14.3/14.4.** Said Targeting is "merged by the page" — it's merged by `withTargeting()`, which the page only awaits. Fixed. (`openspec/changes/keyword-explorer-etsy-traction/tasks.md`)
- [x] 14.6 **Real bug, posted inline — stale comment introduced by round 13's own fix.** Round 13.3 swapped `mockReset()` for `vi.clearAllMocks()` but left a comment explaining the cleanup in terms of the old `mockReset()` behavior ("only touches the Supabase mock"), which no longer matches what the code does. Fixed: reworded to describe what `clearAllMocks()` actually does and doesn't clear (call history, not the mocked implementation), and why `restoreAllMocks()` in `afterEach` is still needed for the `console.error` spy. (`tests/lib/keyword-traction.test.ts`)
- [ ] 14.7 **Investigated, not fixed — a real, accepted limitation of the underlying data, not a defect in this PR's logic.** Finding: round 12's listing-dedup fix (12.2) keys on the exported "Shop/Listing" text, which is a mutable listing title, not a stable Etsy listing ID — a retitled listing could read as two matches, and two distinct listings sharing an identical title would incorrectly collapse into one. Checked the real archived CSVs directly: eRank's Spotted on Etsy export carries no numeric listing ID at all, only this title text (confirmed, not assumed). Resolving a genuinely stable identity would mean a second join against the Etsy API by search term/position per historical pull — well beyond this join's scope, and not something this data source can support today. Documented the limitation directly in `build_ranked_index()`'s docstring, including why it's accepted: a retitle-driven "duplicate" is still a real listing that really ranked, strictly better than the pre-round-12 behavior (every re-pull duplicating every still-ranking listing) rather than a regression from it. (`scripts/ingest-pulls.py`)

Re-ran full suite + lint + typecheck after all of the above: `pnpm exec vitest run` → **1271 passed** / 49 skipped (same 2 pre-existing unrelated shallow-clone failures), `pnpm exec eslint .` → 0 errors (9 pre-existing unrelated warnings elsewhere), `pnpm exec tsc --noEmit` → clean.

## 15. Copilot re-review round (2026-09-18, eleventh pass)

5 findings, 0 posted as separate threads (all suppressed). All five real.

- [x] 15.1 **Real bug, the round's most significant — a gap in round 9's and round 8's own empty-state fixes.** `rangeIsCause`'s predecessor (`rangeBoundSet`) only checked that a bound was actually set (round 9) and a column actually picked (round 8) — but not whether the range filter itself had anything to exclude. If an unrelated filter (keyword/capture/query/status) already emptied the table, a *separately* real range bound still triggered the range-specific "No rows fall within this range" copy, even though the range filter never had a row to exclude in the first place. Fixed: `visible`'s `useMemo` now also computes `withoutRange` (every filter except the range) alongside the fully-filtered result, and the empty-state branch only attributes the empty result to the range when rows existed without it and none survive with it. New test: a real Ranked ≥ 5 bound (which alone would still match rows) set alongside a keyword filter that empties the table on its own — archive-omission copy shows, not the range copy. (`components/KeywordTable.tsx`, `tests/components/KeywordTable.test.tsx`)
- [x] 15.2 **Real doc drift, self-inflicted by round 7's cache addition.** `page.tsx`'s `force-dynamic` doc comment still said "Targeting has to be live on every request" — true before round 7, an overstatement after (up to 60 seconds stale via the cache). Fixed: reworded to say "evaluated at request time," with a new paragraph explaining the cache's bound explicitly rather than leaving the original claim uncorrected. (`app/keyword-explorer/page.tsx`)
- [x] 15.3 **Real doc drift in `tasks.md` task 3.1, same root cause as round 14's findings — one location round 14 missed.** Still said `TargetingMatch | null` (the full server-side shape) is what reaches `KeywordTable`, when round 7.1's `toTableRows()` collapses it to `number | null` on `KeywordTableRow` before the component ever sees it. Fixed: clarified that `KeywordRow` never reaches the component at all. (`openspec/changes/keyword-explorer-etsy-traction/tasks.md`)
- [x] 15.4 **Real test-coverage gap.** The `inputMode="decimal"` switch for the fractional Searches/comp. range column (round 3's fix) had no direct assertion — only the filtering math was tested, so a regression back to the numeric keypad would pass unnoticed. Fixed: new test asserts both range inputs carry `inputmode="decimal"` once that column is selected. (`tests/components/KeywordTable.test.tsx`)
- [x] 15.5 **Real doc drift in `types/index.ts`'s server/client boundary comment — the exact comment documenting the data-exposure boundary itself.** Said "`KeywordRow`, as passed into `KeywordTable`," but `KeywordTable` never receives a `KeywordRow` — only the collapsed `KeywordTableRow`, via `toTableRows()`. Fixed: reworded to name `KeywordTableRow` and `toTableRows()` explicitly, since accuracy here specifically matters (this is the comment a future change would read before deciding what's safe to add to the row shape). (`types/index.ts`)

Re-ran full suite + lint + typecheck after all of the above: `pnpm exec vitest run` → **1273 passed** / 49 skipped (same 2 pre-existing unrelated shallow-clone failures), `pnpm exec eslint .` → 0 errors (9 pre-existing unrelated warnings elsewhere), `pnpm exec tsc --noEmit` → clean.

## 16. Copilot re-review round (2026-09-18, twelfth pass)

4 findings, 0 posted as separate threads (all suppressed). Three real and fixed; one investigated and confirmed a false positive.

- [x] 16.1 **Real hardening, the round's most significant — closes a structural gap the same class of finding kept re-exercising in rounds 3/6/7.** `KeywordTableRow` was defined as `Omit<KeywordRow, "ranked" | "targeting">`, and `toTableRows()` built it with `{ ...row, ranked: ..., targeting: ... }` — a denylist. Any server-only field added to `KeywordRow` in a future change would automatically start flowing to this public, unauthenticated client component, with nothing forcing whoever added it to notice they'd just widened the exposure boundary. Fixed: `KeywordTableRow` is now an explicit field allowlist (nine named fields, no `Omit`), and `toTableRows()` projects each one by name instead of spreading. New test: an input row carrying a field `KeywordTableRow` doesn't know about (simulating exactly the future-field scenario the finding describes) doesn't appear on the result. (`types/index.ts`, `lib/keyword-traction.ts`, `tests/lib/keyword-traction.test.ts`)
- [x] 16.2 **Real doc drift in `design.md` § User flow.** Said Status, Capture and Coverage "are gone from the row entirely in this view" — overstated; the fields stay on the row (Capture and Status still back the existing filter dropdowns), only their *columns* are hidden. This is the same fact round 5.5/6.4 already fixed once in the Visual design table and Decisions section — this was a third location the earlier fixes missed. Fixed: reworded to say "gone from the visible columns," cross-referencing Decisions. (`openspec/changes/keyword-explorer-etsy-traction/design.md`)
- [x] 16.3 **Investigated, not fixed — a false positive.** Finding: `lib/keyword-traction.ts`'s catch-path log "drops the caught error entirely." Checked: `console.error("keyword-explorer: Targeting read failed", error)` passes `error` as a second positional argument, which `console.error` logs in full (message and stack, for an `Error`) — nothing is dropped. Confirmed this is also the codebase's own dominant convention: the identical `console.error("<message>:", error)` shape appears at ~15 other call sites across `lib/` and `app/` (`lib/data.ts`, `lib/landing-page/submit.ts`, most `app/api/**/route.ts` failure handlers). No change made — implementing the suggested fix would make this call site an outlier against an established, working pattern for no real gain.
- [ ] 16.4 **Investigated, not fixed — a real but pre-existing inconsistency, not introduced by this PR.** Finding: the PR description's "OpenSpec artifacts" bullet claims tasks are "all checked except the one requiring Katy's own terminal," but §9.3 and §14.7 (both "investigated, not fixed — accepted limitation/characteristic" findings) are also unchecked, and neither is the terminal walkthrough. Both represent completed investigative work (a decision made and documented, in 14.7's case with a doc/code change) rather than an in-progress implementation task, so leaving them unchecked while calling the checklist "all checked except X" is a real overclaim. Fixed in the PR body itself (not this file, to avoid rewriting historical task records) — the Test plan section now names both explicitly rather than implying only the terminal walkthrough remains.

Re-ran full suite + lint + typecheck after all of the above: `pnpm exec vitest run` → **1274 passed** / 49 skipped (same 2 pre-existing unrelated shallow-clone failures), `pnpm exec eslint .` → 0 errors (9 pre-existing unrelated warnings elsewhere), `pnpm exec tsc --noEmit` → clean.
