## Why

Smoke validation for Experiment Hub schema defaults (`openspec/config.yaml` → `experiment-hub`).

## Experiment links / human anchor

N/A — hub schema pilot tooling run (no funded experiment intent layer).

## Evidence (experiment)

N/A — pilot only.

## Proceed attestation

N/A — non-sponsored hub maintenance / schema validation spike.

## Visual board (FigJam / light Figma)

pending: none for this tooling-only pilot.

## What Changes

- Add disposable OpenSpec change `hub-schema-pilot-20260508` to verify CLI scaffolding and stitched instructions.

## Capabilities

### New Capabilities

- `hub-pilot-demo`: Placeholder capability for verifying `specs/<name>/spec.md` creation.

### Modified Capabilities

- (none)

## Impact

Openspec tooling only; no production code paths.
