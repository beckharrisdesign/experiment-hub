# Landing page — Figma ↔ code inventory

**Verified:** 2026-05-08 — pass 2 (`18:2709` + repo).  
**Pass 3 (reciprocal / DS):** 2026-05-08 — Figma MCP `get_design_context` on `13:128` (DS/header anchor); `use_figma` for footer `80:1268`, Stash text `7:111`, section renames optional badge `80:1274`.

**Scope:** Symbol **Landing Page Content** (`18:2709`). Footer chrome sits **outside** that symbol next to landing on **`Sections`** frame (`13:820`) as frame **`80:1268`** (production parity).

| Region / element          | Figma (node or name)                                                               | Code                                  | Parity status                  | Code Connect            | Notes                                                                                                                                                                      |
| ------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------- | ------------------------------ | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Landing shell             | `18:2709` _Landing Page Content_                                                   | `LandingPage.tsx` composition         | aligned                        | **deferred** (org/plan) | Top-level `@figma` on `LandingPage`; composition split into section files with section-level `@figma` tags.                                                                |
| Hero                      | `7:244` _Section Hero_                                                             | `LandingHero.tsx`                     | **aligned** (after token pass) | deferred                | Figma uses `#166534` (`--green-dark`); was `--brand-primary` in code — updated to match Figma hero.                                                                        |
| Features (“three points”) | `7:48` _Section Three Points — Features_                                           | `LandingFeaturesSection.tsx`          | aligned                        | deferred                | Renamed pass 3 (was duplicate “Section Three Points” vs problem band). Icons SVG vs raster acceptable.                                                                     |
| Pricing                   | `7:84` _Section Pricing_                                                           | `LandingPricingSection.tsx`           | aligned                        | deferred                | Stash tier: **Shipped prototype is SSOT.** Free tier **`50 AI packets/month`** (`7:111` — updated pass 3; code unchanged). Paid: `PricingCard` @ `7:119` / `7:158`.        |
| Stash Starter (free)      | `7:93` Container                                                                   | inline in `LandingPricingSection.tsx` | aligned (copy)                 | deferred                | AI packets line matches **`50`/month** in Figma and code.                                                                                                                  |
| Home Garden (paid)        | `7:119` Container; badge **`80:1274`** _DS / Badge — Most Popular_                 | `PricingCard.tsx`                     | aligned layout + badge frame   | deferred                | Badge frame added pass 3; code keeps `badge="Most popular"`.                                                                                                               |
| Serious Hobby (paid)      | `7:158` Container                                                                  | `PricingCard.tsx`                     | aligned layout                 | deferred                | Shared component with Home Garden.                                                                                                                                         |
| Pricing card UI           | structural match `7:119`                                                           | `PricingCard.tsx`                     | aligned                        | deferred                | `@figma` on featured card node + badge `80:1274`.                                                                                                                          |
| Problem / pain            | `7:8` _Section Three Points — Problem_                                             | `LandingProblemSection.tsx`           | aligned                        | deferred                | Renamed pass 3 for clarity.                                                                                                                                                |
| Signup / CTA              | `18:3287` _Section Signup_; inner panel `7:194`                                    | `LandingSignupSection.tsx`            | aligned                        | deferred                | Outer `--green-dark`; inner `#f9fafb` panel with dark headings; nested white form card + `AuthForm` (`7:202`).                                                             |
| Auth form                 | `7:202` _Container_ (nested)                                                       | `AuthForm.tsx`                        | aligned (embedded)             | deferred                | `@figma` added for inner signup card.                                                                                                                                      |
| Page footer               | `80:1268` _Landing Footer (production parity)_ sibling under `Sections` (`13:820`) | `LandingFooter.tsx` (+ `LandingPage`) | aligned                        | n/a                     | Frame added pass 3; **`@figma 80:1268`**; copy/layout matches prototype footer (gray‑800 chrome, Privacy / Terms / Contact, © line).                                       |
| SSO design-system anchor  | `13:128` Header (DS entry — same symbol as header block)                           | `Header.tsx`                          | aligned                        | deferred                | MCP `get_design_context` confirms node is **`Header`**; no additional standalone main symbols returned under this id for extra React extractions beyond existing `Header`. |

## Figma follow-ups (manual / deferred pass 4+)

- **Promote landing columns / pricing shells to main components:** `createComponentFromNode` rejected for frames **`7:54`**, **`7:13`**, **`7:93`**, **`7:119`**, **`7:158`** while nested under main **`18:2709`** (component internals). Requires **detaching** to a staging frame, **manual Publish → main component**, or editor-side promotion—not automated in pass 3. **Retain** granular frames for traceability (`7:*` IDs unchanged).
- **Badge position:** Decorative **`80:1274`** may need snapping to paid card chrome if pixel polish is desired.
- Optional **duplicate instance cleanup:** If duplicate footers appeared during iteration, consolidate to a single **`80:1268`**.

### Product note — Stash “AI packets” SSOT (**task 1.1**)

- **Decision:** **Shipped prototype** string wins for pass 3: **`50 AI packets/month`** on Stash Starter. Figma text node **`7:111`** updated to match; **`LandingPricingSection`** already matched.

## Code Connect summary

No `.figma.ts` mappings — **Org/plan deferred**. Traceability stays in JSDoc + this table.
