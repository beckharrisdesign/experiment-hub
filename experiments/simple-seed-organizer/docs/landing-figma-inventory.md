# Landing page — Figma ↔ code inventory

**Verified:** 2026-05-08 (Figma MCP `get_design_context` on `S8YJQugvMmn5jaRqwFM5XO:18:2709` + `LandingPage.tsx` in repo).

**Scope:** Symbol **Landing Page Content** (`18:2709`). Sections below list major blocks inside that symbol. Rows outside the symbol are called out explicitly.

| Region / element          | Figma (node or name)                            | Code                                  | Parity status                  | Code Connect            | Notes                                                                                                                                                                                      |
| ------------------------- | ----------------------------------------------- | ------------------------------------- | ------------------------------ | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Landing shell             | `18:2709` _Landing Page Content_                | `LandingPage.tsx` composition         | aligned                        | **deferred** (org/plan) | Top-level `@figma` on `LandingPage`; composition split into section files with section-level `@figma` tags.                                                                                |
| Hero                      | `7:244` _Section Hero_                          | `LandingHero.tsx`                     | **aligned** (after token pass) | deferred                | Figma uses `#166534` (`--green-dark`); was `--brand-primary` in code — updated to match Figma hero.                                                                                        |
| Features (“three points”) | `7:48` _Section Three Points_                   | `LandingFeaturesSection.tsx`          | aligned                        | deferred                | Three-column feature grid; icons SVG in code vs raster in Figma export — acceptable.                                                                                                       |
| Pricing                   | `7:84` _Section Pricing_                        | `LandingPricingSection.tsx`           | **partial**                    | deferred                | **Copy:** Figma Stash tier lists **“5 AI packets/month”** (`7:111`); code says **“50 AI packets/month”** — confirm product source of truth. Paid tiers: `PricingCard` @ `7:119` / `7:158`. |
| Stash Starter (free)      | `7:93` Container                                | inline in `LandingPricingSection.tsx` | partial (copy)                 | deferred                | See AI packets line above.                                                                                                                                                                 |
| Home Garden (paid)        | `7:119` Container                               | `PricingCard.tsx`                     | aligned layout                 | deferred                | “Most popular” badge in code; Figma omits badge frame on snapshot — optional follow-up.                                                                                                    |
| Serious Hobby (paid)      | `7:158` Container                               | `PricingCard.tsx`                     | aligned layout                 | deferred                | Shared component with Home Garden.                                                                                                                                                         |
| Pricing card UI           | structural match `7:119`                        | `PricingCard.tsx`                     | aligned                        | deferred                | **Extracted** to `components/PricingCard.tsx` with `@figma` on featured card node.                                                                                                         |
| Problem / pain            | `7:8` _Section Three Points_                    | `LandingProblemSection.tsx`           | aligned                        | deferred                | Same internal name as features in Figma; different section instance.                                                                                                                       |
| Signup / CTA              | `18:3287` _Section Signup_; inner panel `7:194` | `LandingSignupSection.tsx`            | aligned                        | deferred                | Outer `--green-dark`; inner `#f9fafb` panel with dark headings; nested white form card + `AuthForm` (`7:202`).                                                                             |
| Auth form                 | `7:202` _Container_ (nested)                    | `AuthForm.tsx`                        | aligned (embedded)             | deferred                | `@figma` added for inner signup card.                                                                                                                                                      |
| Page footer               | **not in `18:2709`**                            | `LandingPage.tsx` `<footer>`          | **gap**                        | n/a                     | Footer is app chrome, not in landing symbol — either add to Figma page template or document as out-of-symbol.                                                                              |

## Figma follow-ups (manual)

- **Promote repeated frames to components (optional):** Feature columns (`7:54`–`7:82`), problem columns (`7:13`–`7:41`), and pricing card shells (`7:93`, `7:119`, `7:158`) are still mostly **groups/containers**, not published **main components** — deferred; no batch refactor in this pass.
- **Duplicate “Section Three Points” naming** in Figma (`7:48` vs `7:8`) — rename for clarity when editing the file.

## Code Connect summary

No `.figma.ts` (or equivalent) mappings added — **Org/plan deferred**. Every inventory row records **deferred** or **n/a** so traceability stays in JSDoc + this table.
