---
description: OpenSpec spec-driven changes, openspec/specs capabilities, and when to use them vs experiment PRDs. Apply when editing openspec/, using /opsx commands, or planning hub platform work.
globs: openspec/**/*
---

# OpenSpec workflow (Experiment Hub)

## When to use what

- **OpenSpec lite** (default): **`experiment-hub-lite`** in [`openspec/config.yaml`](openspec/config.yaml) is the spike package — Human anchor, Outcomes, specs (≤5 requirements), design (Figma/breakpoints for UI), tasks §1 = user scenarios. `/opsx:propose` creates **one artifact per approval** when using lite. Strategist conversation → anchor in `proposal.md` → opsx for the rest.
- **BHD experiment** (`schema: bhd-experiment` in `.openspec.yaml`): Product lifecycle artifacts `explore.md` → `propose.md` → `apply.md` → `archive.md`; one artifact per approval. Rules: [`bhd-experiment.mdc`](bhd-experiment.mdc). **Do not** mix lite files (`proposal.md`, `tasks.md`) on the same change. **Code:** child change `--schema experiment-hub-lite` + `/opsx:apply` on child only.
- **PRD track** (optional): `@skills/prd-writer.md` can still produce `experiments/*/docs/PRD.md` for commercial narrative; lite proposal links it instead of duplicating TAM or business case.
- **OpenSpec full** (`schema: experiment-hub` in `.openspec.yaml`): Sponsor Evidence / Proceed, Visual board in proposal, multi-capability hub platform work.
- **OpenSpec vanilla**: `schema: quickstart` or upstream `spec-driven`.
- **Shared hub behavior**: Use `openspec/` for APIs, catalog, cross-experiment flows; link `proposal.md` to `PRD.md` when an experiment drives platform work.

### BHD vs lite (slash commands)

| Command         | Lite / full                            | `bhd-experiment`                                               |
| --------------- | -------------------------------------- | -------------------------------------------------------------- |
| `/opsx:explore` | Freeform thinking                      | Freeform; structured Explore via `/opsx:propose` on BHD change |
| `/opsx:propose` | `proposal.md` → specs → design → tasks | Phase artifacts explore → archive                              |
| `/opsx:apply`   | `tasks.md` implementation + commit/PR  | Child lite change for code; parent = Build Units doc only      |
| `/opsx:archive` | Archive change folder                  | `archive.md` outcome + folder archive                          |

## Git discipline

Match [github-workflow.mdc](github-workflow.mdc): one branch per logical unit named `<harness>/<descriptor>` for the assisting tool (e.g. `claude/<descriptor>` under Claude Code — not a hardcoded `cursor/`), one PR, agent does not merge or approve. Align an active change folder name with that branch when practical.

**`/opsx:apply`:** Implement tasks; when apply **finishes**, commit + push + a **ready-for-review** PR (not draft, so Copilot reviews immediately — see [`github-workflow.mdc`](github-workflow.mdc)) per the active schema’s **`apply.instruction`** (returned by `openspec instructions apply --json`) and [`skills/openspec-apply-change.md`](../skills/openspec-apply-change.md) step 8. **`/opsx:propose`** does not commit; artifacts stay local until apply.

## Scope pivots

When Katy redirects a change's scope mid-flight (e.g. "rewrite the page" becomes "remove the page"):

- **Rename the change directory** to describe what the change now does (`git mv openspec/changes/<old> openspec/changes/<new>`) — the change id should stay truthful, not historical.
- **Rewrite the proposal** to the new scope, and quote the redirecting sentence **verbatim** as the new (or leading) Human anchor — the pivot instruction is founder intent.
- **Record the pivot in the proposal**, one or two lines: what it began as, what redirected it, and where superseded work lives (Figma pages, Notion docs) so nothing explored is lost.
- **Retitle/re-body the open PR** to match; superseded design artifacts stay in their file as a parked record, not deleted.

(Precedent: `method-page` → `remove-workflow-page`, 2026-07-21.)

## Figma and `design.md`

UI-impacting changes MUST document design context in `design.md` (see [figma.mdc](figma.mdc) § OpenSpec).

## Single source of truth (opsx workflow)

Edit OpenSpec agent workflow **only** under [`skills/`](../skills/):

| Slash command   | Skill file                                                                  |
| --------------- | --------------------------------------------------------------------------- |
| `/opsx:propose` | [`skills/openspec-propose.md`](../skills/openspec-propose.md)               |
| `/opsx:apply`   | [`skills/openspec-apply-change.md`](../skills/openspec-apply-change.md)     |
| `/opsx:archive` | [`skills/openspec-archive-change.md`](../skills/openspec-archive-change.md) |
| `/opsx:explore` | [`skills/openspec-explore.md`](../skills/openspec-explore.md)               |

`.cursor/commands/opsx-*.md` are stubs that point here — do not duplicate steps. After clone: `bash scripts/link-agent-dirs.sh` (symlinks `.cursor/skills` and `.claude/skills` → `skills/`).

## Artifacts output (all opsx commands)

Agents MUST end responses with `## Artifacts` when they create, update, or move files under `openspec/changes/`. Build links from OpenSpec CLI JSON (`changeDir`, `outputPath`, globs, `contextFiles`, `openspec status --json`) so the rule works for every schema. Full rules: [`skills/openspec-artifacts-output.md`](../skills/openspec-artifacts-output.md).

## Archiving

When a change is merged, move `openspec/changes/<name>/` to `openspec/changes/archive/YYYY-MM-DD-<name>/` (or use `/opsx:archive`). Canonical specs under `openspec/specs/` should already reflect the merged behavior.
