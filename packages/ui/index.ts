// Generic UI primitives safe to use outside the hub shell.
// Hub-specific components (Header, Footer, Sidebar, ExperimentList, etc.)
// stay in /components — add exports here only when a prototype needs them.
export { default as Button } from "../../components/Button";
export { default as Tabs } from "../../components/Tabs";
export { default as Tooltip } from "../../components/Tooltip";
export { default as StatusBadge } from "../../components/StatusBadge";
export { default as ScoreBadge } from "../../components/ScoreBadge";
